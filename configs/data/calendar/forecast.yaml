_target_: src.datasets.calendar.ForecastDataset
_partial_: True
targets: ${targets}
forecast_horizon: ${forecast_horizon}
eu_countries_path: ${paths.opsd_data_dir}/eu_countries.json
start_date: ${train_start}
end_date: ${test_end}