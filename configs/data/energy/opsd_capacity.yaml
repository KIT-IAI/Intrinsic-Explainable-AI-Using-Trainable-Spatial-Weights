_target_: src.datasets.energy.OPSDCapacityDataset
_partial_: True
data_path: ${paths.opsd_data_dir}/opsd.csv
targets: ${targets}
historical_lag: ${historical_lag}
forecast_horizon: ${forecast_horizon}
normalize_method: standard