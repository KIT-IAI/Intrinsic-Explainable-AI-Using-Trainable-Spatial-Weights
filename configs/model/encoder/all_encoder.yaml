_target_: src.models.energy_forecasting.encoder.LatentEncoder
energy_architecture: fcn
latent_energy_size: ${forecast_horizon}
weather_architecture: fcn
latent_weather_size: ${forecast_horizon}
weather_horizon: ${data.weather.weather_horizon}
upper_left: ${data.weather.upper_left}
lower_right: ${data.weather.lower_right}
scaling: ${data.weather.scaling}
calendar_architecture: fcn
latent_calendar_size: ${forecast_horizon}